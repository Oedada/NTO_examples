> Краткое описание (1–2 предложения):  Что делает библиотека и зачем она нужна.

---

## 📦 Общая информация

- **Категория:** CLI
- **Совместимость:** Python ≥ 3.2
- **Документация:** [Docs](https://docs.python.org/3/library/argparse.html)

---

## ⚙️ Установка

```bash
# предустановлен
```

---

## Основные возможности

- [ ] Парсинг аргументов 
- [ ] Проверка правильного ввода аргументов
- [ ] Автоматическое создание help

---

## Основные классы и функции

| Объект                                        | Тип     | Аргументы                                                                 | Описание аргументов                                                                                                                                                                                                                                     | Возвращает               | Краткое описание                                                                                                 |
| --------------------------------------------- | ------- | ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ | ---------------------------------------------------------------------------------------------------------------- |
| `ArgumentParser`                              | класс   | description                                                               | описание показывается при help                                                                                                                                                                                                                          | instance                 | Создаёт объект класса                                                                                            |
| `ArgumentParser.add_argument`                 | функция | 1) name_or_flag<br>2) help<br>3) type<br>4) default<br>5)action<br>6)dest | Имена опции/флага(может быть несколько)<br>Сообщение отображаемое в help<br>Тип аргумента в программе<br>Стандартное значение(арг. становится не обяз.)<br>Задаёт опрделённое действие(см. ниже)<br>Задаёт переменную, в которую будет записан арг.<br> | `result`                 | Добавляет аргумент                                                                                               |
| `ArgumentParser.add_mutually_exclusive_group` | функция | required                                                                  | При True аргументы становятся обязательными                                                                                                                                                                                                             | `MutuallyExclusiveGroup` | Создаёт группу, где <br>все аргументы <br>конфликтуют между <br>собой<br>(не допускается <br>одновременное исп.) |

---
#### Action в ArgumentParser.add_argument

| Значение                   | Что делает                               | Пример                                                                |
| -------------------------- | ---------------------------------------- | --------------------------------------------------------------------- |
| `"store"` _(по умолчанию)_ | сохраняет переданное значение            | `--name Alex → args.name = "Alex"`                                    |
| `"store_true"`             | если флаг указан → `True`, иначе `False` | `--debug`                                                             |
| `"store_false"`            | наоборот: если флаг есть → `False`       | `--no-cache`                                                          |
| `"append"`                 | добавляет значение в список              | `-t py -t md → args.t = ["py", "md"]`                                 |
| `"count"`                  | считает количество повторений            | `-v -vv → args.v = 2`                                                 |
| `"help"`                   | выводит справку и завершает программу    | автоматически для `--help`                                            |
| `"version"`                | выводит версию и завершает               | `parser.add_argument('--version', action='version', version='1.0.0')` |

---

## Примеры использования

### Простой пример
```python
parser = argparse.ArgumentParser()

group = parser.add_mutually_exclusive_group(required=True)
group.add_argument("--add", action="store_true")
group.add_argument("--remove", action="store_true")

args = parser.parse_args()
```

### Расширенный пример
```python
import argparse
parser = argparse.ArgumentParser(description="Пример работы с аргументами")

parser.add_argument("file", help="Путь к файлу") # обязательный
parser.add_argument("-v", "--verbose", action="store_true", help="Вывести дополнительную информацию")
parser.add_argument("-n", "--number", type=int, default=10, help="Количество повторений")
args = parser.parse_args()

print(f"Файл: {args.file}")
print(f"Verbose: {args.verbose}")
print(f"Number: {args.number}")
```


## Советы и паттерны

> ✨ Личные заметки, нестандартные фишки, обход багов, лучшие практики.

- Можно не использовать dest, он положит аргумент в переменную с именем последнего длинного имени аргумента
- Для отладки можно использовать `import logging`  
- В больших проектах — интегрировать с `typer` или `click`

---

## Интеграция

- Совместима с: **sys**, 
- Подходит для: скриптов на python
- Работает под Linux, Windows, mac OS  

---

## Полезные ссылки

- 

---

## Чеклист

- [ ] 

---
