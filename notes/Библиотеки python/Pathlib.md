> **Современный, объектно-ориентированный модуль для работы с путями в файловой системе**

---

## 📦 Общая информация

- **Категория:** (например: Web, Async, Data, CLI, ML, etc.)
- **Совместимость:** Python ≥ 3.6
- **Документация:** [Docs](https://docs.python.org/3/library/pathlib.html)

---

## ⚙️ Установка

```bash
# предустановлен
```

---

## Основные возможности

- [ ] Предоставляет более удобное представление пути
- [ ] Производство операций с путями

---

## Основные классы и функции

| Объект                    | Тип      | Аргументы                        | Возвращает       | Краткое описание                                                                                                                                                                                                                                                           |
| ------------------------- | -------- | -------------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Path()`                  | класс    | `path: str`                      | instance         | Объект пути к path                                                                                                                                                                                                                                                         |
| `expanduser()`            | метод    | -                                | `Path`           | Заменяет ~ на /home/name(linux)                                                                                                                                                                                                                                            |
| `/`                       | операция | -                                | `Path`           | Добавляет в конец пути новый путь                                                                                                                                                                                                                                          |
| `suffix`                  | атрибут  | -                                | `str`            | Последняя строка после точки(посл. расширение)                                                                                                                                                                                                                             |
| `suffixes`                | атрибут  | -                                | `list[str]`      | Все строки после всех точек(все расширения)                                                                                                                                                                                                                                |
| `stem`                    | атрибут  | -                                | `Path`           | Возвращает путь к файлу без suffix(не suffixes!)                                                                                                                                                                                                                           |
| `with_suffix()`           | метод    | `suffix: str`                    | `Path`           | Заменяет Path().suffix на suffix в пути                                                                                                                                                                                                                                    |
| `with_name()`             | метод    | `name: str`                      | `Path`           | Файл с именем name в той же директории                                                                                                                                                                                                                                     |
| `exists()`                | метод    | -                                | `bool`           | Существует ли                                                                                                                                                                                                                                                              |
| `is_file()`               | метод    | -                                | `bool`           | Файл ли это                                                                                                                                                                                                                                                                |
| `is_dir()`                | метод    | -                                | `bool`           | Директория ли это                                                                                                                                                                                                                                                          |
| `stat()`                  | метод    | -                                | `os.stat_result` | Информация о пути(подробнее - [рус](https://docs-python.ru/standart-library/modul-os-python/obekt-stat-result-modulja-os/), [англ](https://docs.python.org/3/library/os.html#os.stat_result), [а также](https://docs.python.org/3/library/pathlib.html#pathlib.Path.stat)) |
| `parts`                   | атрубут  | -                                | `tuple`          | Кортеж компонентов пути                                                                                                                                                                                                                                                    |
| `parent`                  | атрубут  | -                                | `Path`           | Родительский каталог                                                                                                                                                                                                                                                       |
| `parents`                 | атрубут  | -                                | `Path`           | Родители в порядке убывания глубины                                                                                                                                                                                                                                        |
| `name`                    | атрубут  | -                                | `str`            | Имя файла(последнего компонента)                                                                                                                                                                                                                                           |
| `drive`                   | атрубут  | -                                | `str`            | Имя диска(только для windows)                                                                                                                                                                                                                                              |
| `write_text()`            | метод    | `string: str, encoding: str`     | ?                | Перезаписывает текст в файл                                                                                                                                                                                                                                                |
| `read_text()`             | метод    | `encoding: str`                  | `string: str`    | Читает текст из файла                                                                                                                                                                                                                                                      |
| `write/read_bytes()`      | метод    | аналогично                       | аналогично       | аналогично                                                                                                                                                                                                                                                                 |
| `open()`                  | метод    | `mode: str, encoding: str`       | Обёртка          | Контекстный менеджер, как обычный open                                                                                                                                                                                                                                     |
| `mkdir()`                 | метод    | `parents: bool, exists_ok: bool` | -                | Создание каталога<br>parents - создать рекурсивно<br>exists_ok - не бросает исключ., если есть                                                                                                                                                                             |
| `touch()`                 | метод    | `exists_ok: bool`                | -                | Создаёт пустой файл                                                                                                                                                                                                                                                        |
| `unlink()`                | метод    | -                                | -                | Удалить файл                                                                                                                                                                                                                                                               |
| `rmdir()`                 | метод    | -                                | -                | Удалить пустой каталог                                                                                                                                                                                                                                                     |
| `shutil.rmtree('my_dir')` | метод    | `dir: str`                       | -                | Удалить каталог рекурсивно                                                                                                                                                                                                                                                 |
| `replace()`               | метод    | `path: str`                      | -                | Заменить файл на path файлом с Path()                                                                                                                                                                                                                                      |
| `rename()`                | метод    | `path: str`                      | -                | Переименовать файл Path()                                                                                                                                                                                                                                                  |
| `iterdir()`               | метод    | -                                | `map object`     | Проходится по директории(не рекурсивно)                                                                                                                                                                                                                                    |
| `glob()`                  | метод    | `pattern: str`                   | `map object`     | Файлы в Path() подходящие под шаблон                                                                                                                                                                                                                                       |
| `relative_to()`           | метод    | `base: Path`                     | `Path`           | Путь Path() относительно base                                                                                                                                                                                                                                              |
| `resolve()`               | метод    | -                                | `Path`           | Абсолютный путь файла(следует симлинки)                                                                                                                                                                                                                                    |
| `absolute()`              | метод    | -                                | `Path`           | То же самое, но не следует симлинки                                                                                                                                                                                                                                        |
| `chmod()`                 | метод    | `mode: int`                      | -                | Установить режим(только для unix, подробнее [здесь](https://docs.python.org/3/library/pathlib.html#pathlib.Path.chmod))                                                                                                                                                    |

---

## Примеры использования

## Path(PurePath) и expanduser
### Простой пример
```python
from pathlib import Path, PurePath

p = Path('~/projects').expanduser()   # concrete Path (потребует expanduser, чтобы заменить ~)
p2 = Path('/etc/passwd')               # абсолютный путь
pp = PurePath('some', 'path', 'file')  # pure path, без доступа к FS
```

## Операции
### Пример 1
```python
base = Path('/home/user')
p = base / 'documents' / 'notes.txt'    # оператор /
p2 = base.joinpath('documents', 'notes.txt')  # эквивалент joinpath
```
### Пример 2
```python
p = Path('archive.tar.gz')
p.suffix           # '.gz'
p.suffixes         # ['.tar', '.gz']
p.stem             # 'archive.tar'  (в отличие от name без расширения)
p.with_suffix('.zip')  # Path('archive.tar.zip')
p2 = Path('report.txt')
p2.with_name('summary.txt')  # Path('summary.txt') в той же директории

```

## Информация о пути
```python
p = Path('/etc/hosts')
p.exists()      # True/False
p.is_file()
p.is_dir()
p.stat()        # возвращает os.stat_result: размер, время модификации и т.д.
p.parts         # кортеж компонентов пути, напр. ('/', 'etc', 'hosts') или ('C:\\','Users',...)
p.parent        # родительский каталог (Path)
p.parents       # последовательность родителей
p.name          # имя файла (последний компонент)
p.drive         # только для Windows: 'C:'
```

## Чтение и запись
```python
p = Path('notes.txt')

# запись текста (строка)
p.write_text("Привет, мир!\n", encoding='utf-8')

# чтение текста
text = p.read_text(encoding='utf-8')

# запись байтов
p_bin = Path('data.bin')
p_bin.write_bytes(b'\x00\x01\x02')

# чтение байтов
b = p_bin.read_bytes()

```

## Работа с open()
```python
p = Path('bigfile.log')
with p.open('r', encoding='utf-8') as f:
    for line in f:
        process(line)

```

## Создание и удаление каталогов / файлов
```python
d = Path('my_dir/subdir')

d.mkdir(parents=True, exist_ok=True)  # создает рекурсивно, не бросает исключение если уже есть

# создать пустой файл (аналог touch)
file = d / 'file.txt'
file.touch(exist_ok=True)

# удалить файл
file.unlink()   # аналог os.remove

# удалить пустой каталог
d.rmdir()

# удалить и содержимое — используйте shutil.rmtree
import shutil
shutil.rmtree('my_dir')
```

## Перемещение и переименование
```python
p = Path('oldname.txt')
p.replace('newname.txt')   # атомарное перемещение/переименование (если возможно)
p.rename('new2.txt')       # тоже возможно; поведение почти одинаковое
'''
Пояснение:
- replace() более безопасен, т.к. может перезаписать целевой файл.
'''
```

## Обход директорий
```python
p = Path('project')

# Простой перебор элементов в директории (не рекурсивно)
for child in p.iterdir():
    print(child.name, child.is_file())

# glob: шаблоны с shell-символами
for py in p.glob('**/*.py'):  # '**' + recursive
    print(py)

# rglob — рекурсивный glob (синоним p.glob('**/pattern'))
for txt in p.rglob('*.txt'):
    print(txt)

```

## Относительные пути
```python
p = Path('/home/user/project/src/main.py')
base = Path('/home/user/project')

p.relative_to(base)   # Path('src/main.py')
p = Path('some/../path/./file.txt')
p.resolve()    # возвращает абсолютный канонизированный путь (следует симлинки)
p.absolute()   # возвращает абсолютный путь без разрешения симлинков (Path.absolute() не всегда доступен в старых версиях)

```

## Права и статистика
```python
p = Path('script.sh')
p.chmod(0o755)       # установить режим (Unix)
st = p.stat()
st.st_mode, st.st_size, st.st_mtime
```
---


## Советы и паттерны

> ✨ Личные заметки, нестандартные фишки, обход багов, лучшие практики.

- Используй `with` для корректного закрытия ресурсов  
- Для отладки можно использовать `import logging`  
- В больших проектах — интегрировать с `typer` или `click`

---

## Интеграция

- Совместима с: **subprocces**, **os**,  Многие либы могут работать с Path, вместо строковых путей.
- Подходит для: судобного взаимодействия с путями без лишних заморочек
- Работает под Linux, Windows, mac OS  

---

## Полезные ссылки

- 

---

## Чеклист

- [ ] 

---
